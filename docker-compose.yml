version: '3.8'

services:
  # Redis Cache
  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped
    networks:
      - coolify

  # Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    expose:
      - "8000"
    ports:
      - "8010:8000"
    environment:
      - REDIS_URL=redis://redis:6379
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
      - DATAFORSEO_LOGIN=${DATAFORSEO_LOGIN}
      - DATAFORSEO_PASSWORD=${DATAFORSEO_PASSWORD}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_ORIGINS=["http://localhost:3000", "http://frontend:3000", "https://${COOLIFY_FQDN}", "https://n8n.giniloh.com", "https://scout.buildomain.com"]
      - ALLOWED_HOSTS=["*"]
    depends_on:
      - redis
    restart: unless-stopped
    networks:
      - coolify

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - REACT_APP_API_URL=/api/v1
        - REACT_APP_SUPABASE_URL=${SUPABASE_URL}
        - REACT_APP_SUPABASE_ANON_KEY=${SUPABASE_KEY}
        - REACT_APP_URL=https://scout.buildomain.com
    expose:
      - "80"
    labels:
      - "traefik.enable=true"
      # Secure Router
      - "traefik.http.routers.scout-router-secure.rule=Host(`scout.buildomain.com`)"
      - "traefik.http.routers.scout-router-secure.entrypoints=https"
      - "traefik.http.routers.scout-router-secure.tls=true"
      - "traefik.http.routers.scout-router-secure.tls.certresolver=letsencrypt"
      - "traefik.http.services.scout-router-secure.loadbalancer.server.port=80"
      # HTTP -> HTTPS Redirect
      - "traefik.http.routers.scout-router-http.rule=Host(`scout.buildomain.com`)"
      - "traefik.http.routers.scout-router-http.entrypoints=http"
      - "traefik.http.routers.scout-router-http.middlewares=scout-v3-redirect"
      - "traefik.http.middlewares.scout-v3-redirect.redirectscheme.scheme=https"
      - "traefik.docker.network=coolify"
    environment:
      - REACT_APP_API_URL=/api/v1
    depends_on:
      - backend
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost/health" ]
      interval: 5s
      timeout: 2s
      retries: 20
      start_period: 10s
    networks:
      - coolify

volumes:
  redis_data:
    driver: local

networks:
  coolify:
    external: true
